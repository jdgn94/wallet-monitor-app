package app.jdgn.wallet_monitor.utils

fun formatNumber(number: Long, decimalPlaces: Int) {
    val numberString = number.toString()
    val decimalIndex = numberString.length - decimalPlaces.toInt()
}